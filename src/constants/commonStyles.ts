import { StyleSheet } from 'react-native';

/**
 * Common reusable styles shared across multiple screens
 */
export const CommonStyles = StyleSheet.create({
  flexContainer: {
    flex: 1,
  },
  contentPadding: {
    paddingHorizontal: 20,
  },
});
